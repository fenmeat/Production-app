:root {
	/* Functional/status colors — unchanged, these convey meaning (not brand decoration) */
	--red: #C0392B;
	--green: #27AE60;
	--orange: #E67E22;

	/* Elevated Precision — FenMeat's implementation (see Master Context Doc Section 8) */
	--charcoal: #12141C;   /* dark base — header, dark surfaces */
	--steel: #262B38;      /* secondary dark tone, paired with Charcoal in gradients */
	--blue: #003CFF;       /* Royal Blue — brand accent, used SPARINGLY, not flooded */
	--blue-deep: #0026B3;  /* Royal Blue Deep — gradient partner for the accent */
	--platinum: #F6F5F1;   /* main light background, warmer than plain white/grey */
	--silver: #C9CDD6;     /* muted text/dividers on dark surfaces */

	/* Legacy aliases kept so existing rules referencing var(--dark)/var(--platinum) below
	   still resolve sensibly to the new palette without needing every single rule rewritten */
	--dark: var(--charcoal);

	--mid: #2d2d2d;
	--card: #f9f9f9;
	--border: #e3e1da;
	--text: #24262E;
	--muted: #767a85;
}

@import url('https://fonts.googleapis.com/css2?family=Poppins:wght@400;500;600;700;800&display=swap');

* { box-sizing: border-box; margin: 0; padding: 0; }

html {
	-webkit-text-size-adjust: 100%;
	text-size-adjust: 100%;
}
body {
	font-family: 'Poppins', -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
	background: var(--platinum);
	color: var(--text);
	-webkit-tap-highlight-color: transparent;
	font-variant-numeric: tabular-nums;
}

.header {
	background: linear-gradient(135deg, var(--charcoal), var(--steel));
	color: white;
	padding: 8px 16px;
	display: flex;
	justify-content: space-between;
	align-items: center;
	position: relative;
}
.header::after {
	content: '';
	position: absolute;
	bottom: 0; left: 0; right: 0;
	height: 1px;
	background: linear-gradient(90deg, transparent, rgba(255,255,255,0.15), transparent);
}
.header-logo { height: 36px; display: block; }
.header-divider { width: 1px; height: 28px; background: var(--silver); opacity: 0.35; }
.header-app-name { font-size: 15px; font-weight: 500; color: var(--silver); letter-spacing: 0.3px; }
.header-right { font-size: 12px; color: var(--silver); text-align: right; }

.date-bar {
	background: white;
	padding: 12px 16px;
	display: flex;
	align-items: center;
	gap: 10px;
	border-bottom: 1px solid var(--border);
}
.date-bar label { font-size: 14px; font-weight: 600; }
.date-bar input[type="date"] {
	flex: 1;
	padding: 10px;
	font-size: 16px;
	border: 1px solid var(--border);
	border-radius: 6px;
}

.mode-bar {
	display: flex;
	background: var(--mid);
}
.mode-btn {
	flex: 1;
	padding: 14px 8px;
	border: none;
	background: var(--mid);
	color: #ccc;
	font-size: 14px;
	font-weight: 700;
	cursor: pointer;
}
.mode-btn.active.plan { background: linear-gradient(135deg, var(--blue), var(--blue-deep)); color: white; }
.mode-btn.active.actuals { background: linear-gradient(135deg, var(--charcoal), var(--steel)); color: white; }

.subtab-bar {
	display: flex;
	overflow-x: auto;
	background: white;
	border-bottom: 1px solid var(--border);
	-webkit-overflow-scrolling: touch;
}
.subtab-btn {
	flex: 1;
	min-width: 76px;
	padding: 12px 6px;
	border: none;
	background: white;
	color: var(--muted);
	font-size: 12px;
	font-weight: 700;
	letter-spacing: 0.3px;
	border-bottom: 3px solid transparent;
	cursor: pointer;
	white-space: nowrap;
}
.subtab-btn.active {
	color: var(--blue);
	border-bottom-color: var(--blue);
	background: var(--platinum);
}

.needs-input {
	border-color: var(--orange) !important;
	background: #FFF3E0 !important;
}

.prow {
	background: white;
	border-bottom: 1px solid var(--border);
	padding: 14px 16px;
}
.prow-header {
	display: flex;
	align-items: center;
	gap: 10px;
	cursor: pointer;
}
.prow-name-wrap { flex: 1; min-width: 0; }
.prow-name { font-size: 15px; font-weight: 700; }
.prow-code { font-size: 11px; color: var(--muted); margin-left: 6px; font-weight: 600; }
.prow-nums { display: flex; gap: 16px; }
.prow-num { text-align: center; min-width: 40px; }
.prow-num-label {
	display: block;
	font-size: 10px;
	color: var(--muted);
	text-transform: uppercase;
	letter-spacing: 0.3px;
	font-weight: 700;
}
.prow-num-val { display: block; font-size: 17px; font-weight: 700; }
.prow-status {
	min-width: 92px;
	text-align: right;
	font-size: 13px;
	font-weight: 700;
	white-space: nowrap;
}
.prow-status.ok { color: var(--green); }
.prow-status.produce { color: var(--orange); }
.prow-status-wrap { text-align: right; }
.prow-status-batches {
	font-size: 10px;
	color: var(--muted);
	margin-top: 1px;
}
.prow-chevron {
	color: var(--muted);
	font-size: 11px;
	transition: transform 0.15s;
	flex-shrink: 0;
}
.prow.expanded .prow-chevron { transform: rotate(180deg); }
.prow-detail {
	display: none;
	margin-top: 14px;
	padding-top: 14px;
	border-top: 1px dashed var(--border);
}
.prow.expanded .prow-detail { display: block; }
.prow-detail-grid {
	display: grid;
	grid-template-columns: 1fr 1fr;
	gap: 12px;
}
.prow-detail-field label {
	display: block;
	font-size: 11px;
	color: var(--muted);
	margin-bottom: 4px;
	font-weight: 700;
	text-transform: uppercase;
	letter-spacing: 0.3px;
}

.context-bar {
	background: var(--platinum);
	border-bottom: 1px solid var(--border);
	padding: 10px 16px;
	font-size: 13px;
	color: var(--dark);
}
.context-bar .routes { font-weight: 700; }
.context-bar .period {
	display: inline-block;
	margin-left: 8px;
	padding: 2px 8px;
	border-radius: 10px;
	background: var(--blue);
	color: white;
	font-size: 11px;
	font-weight: 700;
}
.src-badge {
	display: inline-block;
	padding: 2px 8px;
	border-radius: 10px;
	font-size: 11px;
	font-weight: 700;
	margin-right: 6px;
}
.src-badge.confirmed { background: var(--green); color: white; }
.src-badge.forecast { background: #ddd; color: var(--text); }

.content { padding-bottom: 90px; }

.loading {
	text-align: center;
	padding: 60px 20px;
	color: var(--muted);
}
.spinner {
	width: 28px; height: 28px;
	border: 3px solid var(--border);
	border-top-color: var(--blue);
	border-radius: 50%;
	margin: 0 auto 12px;
	animation: spin 0.8s linear infinite;
}
@keyframes spin { to { transform: rotate(360deg); } }

.summary-bar {
	background: white;
	display: flex;
	border-bottom: 1px solid var(--border);
}
.summary-item {
	flex: 1;
	text-align: center;
	padding: 14px 8px;
	border-right: 1px solid var(--border);
}
.summary-item:last-child { border-right: none; }
.summary-val { font-size: 22px; font-weight: 700; }
.summary-label { font-size: 11px; color: var(--muted); margin-top: 2px; text-transform: uppercase; letter-spacing: 0.4px; }

.product-header {
	display: grid;
	grid-template-columns: 1fr 80px;
	gap: 8px;
	padding: 8px 16px;
	background: #ececec;
	font-size: 11px;
	font-weight: 700;
	color: var(--muted);
	text-transform: uppercase;
	letter-spacing: 0.4px;
}
.product-header.actuals-mode { grid-template-columns: 1fr 55px 65px 65px 55px; }
.product-header.plan-mode { grid-template-columns: 1fr 58px 58px 58px 58px; }

.product-row {
	display: grid;
	grid-template-columns: 1fr 80px;
	gap: 8px;
	align-items: center;
	padding: 12px 16px;
	background: white;
	border-bottom: 1px solid var(--border);
}
.product-row.actuals-mode { grid-template-columns: 1fr 55px 65px 65px 55px; }
.product-row.plan-mode { grid-template-columns: 1fr 58px 58px 58px 58px; }
.product-row:nth-child(even) { background: #fafafa; }

.product-name { font-size: 14px; font-weight: 600; }
.product-code { font-size: 11px; color: var(--muted); margin-top: 1px; }

.plan-qty {
	text-align: center;
	font-size: 18px;
	font-weight: 700;
	color: var(--blue);
}

.qty-input {
	width: 100%;
	padding: 8px 4px;
	font-size: 16px;
	text-align: center;
	border: 1px solid var(--border);
	border-radius: 6px;
}
.qty-input:focus { outline: none; border-color: var(--blue); }

.produce-qty {
	text-align: center;
	font-size: 18px;
	font-weight: 700;
	color: var(--orange);
}
.produce-input {
	text-align: center;
	font-size: 16px;
	font-weight: 700;
	color: var(--orange);
	border-color: var(--orange);
}
.produce-batches {
	text-align: center;
	font-size: 10px;
	color: var(--muted);
	font-weight: 600;
	margin-top: 1px;
}
.produce-input-row {
	display: flex;
	gap: 6px;
	align-items: stretch;
}
.produce-input-row .produce-input { flex: 1; min-width: 0; }
.add-batch-btn {
	flex-shrink: 0;
	padding: 0 10px;
	font-size: 12px;
	font-weight: 700;
	color: white;
	background: var(--orange);
	border: none;
	border-radius: 6px;
	white-space: nowrap;
	cursor: pointer;
}
.add-batch-btn:active { opacity: 0.8; }

.surplus-qty {
	text-align: center;
	font-size: 16px;
	font-weight: 700;
}
.surplus-pos { color: var(--green); }
.surplus-neg { color: var(--red); }
.surplus-zero { color: var(--muted); }

.empty-state {
	text-align: center;
	padding: 60px 24px;
	color: var(--muted);
}
.empty-state .big { font-size: 32px; margin-bottom: 8px; }

.trolley-card {
	background: white;
	border: 1px solid var(--border);
	border-radius: 10px;
	margin: 12px 16px;
	padding: 14px 16px;
	position: relative;
	box-shadow: 0 1px 2px rgba(18,20,28,0.05), 0 4px 14px rgba(18,20,28,0.07);
}
.trolley-card::before {
	content: '';
	position: absolute;
	top: 0; left: 10px; right: 10px;
	height: 1px;
	background: linear-gradient(90deg, transparent, rgba(0,60,255,0.35), transparent);
}
.trolley-title {
	font-size: 13px;
	font-weight: 700;
	color: var(--blue);
	margin-bottom: 10px;
}
.trolley-row {
	display: flex;
	justify-content: space-between;
	font-size: 13px;
	padding: 3px 0;
	color: var(--text);
}
.trolley-row b { color: var(--dark); }
.trolley-split {
	display: flex;
	gap: 8px;
	margin-top: 10px;
}
.trolley-split > div {
	flex: 1;
	background: white;
	border-radius: 6px;
	padding: 8px 4px;
	text-align: center;
}
.trolley-split span {
	display: block;
	font-size: 11px;
	color: var(--muted);
	font-weight: 700;
}
.trolley-split b {
	display: block;
	font-size: 18px;
	color: var(--dark);
}
.trolley-leftover {
	margin-top: 10px;
	font-size: 12px;
	color: var(--muted);
	font-style: italic;
}

.trolley-coldroom-input {
	display: flex;
	justify-content: space-between;
	align-items: center;
	gap: 10px;
	background: white;
	border: 1px solid var(--blue);
	border-radius: 6px;
	padding: 8px 10px;
	margin: 8px 0;
}
.trolley-coldroom-input span {
	font-size: 13px;
	font-weight: 600;
	color: var(--dark);
}
.trolley-coldroom-input small {
	font-weight: 400;
	color: var(--muted);
	display: block;
	font-size: 11px;
}
.trolley-coldroom-input input {
	width: 70px;
	flex-shrink: 0;
}

.trolley-extra {
	background: white;
	border: 1px solid var(--blue);
	border-radius: 6px;
	padding: 8px 10px;
	margin-top: 10px;
}
.trolley-extra-over {
	border-color: var(--red);
	background: #FDECEA;
}
.trolley-extra-title {
	font-size: 12px;
	font-weight: 700;
	color: var(--dark);
	margin-bottom: 6px;
}
.trolley-extra-over .trolley-extra-title { color: #8a2419; }
.trolley-extra-note {
	font-size: 11px;
	color: var(--muted);
	margin: 4px 0 6px;
	font-style: italic;
}

.polony-card {
	background: white;
	border: 1px solid var(--border);
	border-radius: 10px;
	margin: 0 16px 12px;
	padding: 14px 16px;
	position: relative;
	box-shadow: 0 1px 2px rgba(18,20,28,0.05), 0 4px 14px rgba(18,20,28,0.07);
}
.polony-card::before {
	content: '';
	position: absolute;
	top: 0; left: 10px; right: 10px;
	height: 1px;
	background: linear-gradient(90deg, transparent, rgba(0,60,255,0.35), transparent);
}
.polony-title {
	font-size: 13px;
	font-weight: 700;
	color: var(--blue);
	margin-bottom: 10px;
}
.pc-header, .pc-row {
	display: grid;
	grid-template-columns: 1fr 60px 60px 60px;
	gap: 4px;
	padding: 3px 0;
	font-size: 13px;
}
.pc-label { color: var(--muted); }
.pc-val { text-align: center; }
.pc-header .pc-val { font-weight: 700; font-size: 12px; color: var(--blue); line-height: 1.3; }
.pc-divider { border-top: 1px solid var(--border); margin: 6px 0; }
.pc-row-highlight {
	background: white;
	border-radius: 6px;
	padding: 4px 6px;
	margin: 2px 0;
}
.pc-cook { color: var(--orange); }
.pc-ok { color: var(--green); }
.polony-note {
	margin-top: 10px;
	font-size: 12px;
	color: var(--muted);
	font-style: italic;
}

.pt-title {
	font-size: 13px;
	font-weight: 700;
	color: var(--blue);
	margin-bottom: 8px;
}
.pt-title small {
	font-weight: 400;
	color: var(--muted);
	font-size: 11px;
}
.pt-progress {
	background: white;
	border-radius: 6px;
	padding: 8px 10px;
	margin-bottom: 10px;
}
.pt-progress-row {
	display: flex;
	justify-content: space-between;
	font-size: 13px;
	padding: 2px 0;
}
.pt-header {
	display: grid;
	grid-template-columns: 16px 1fr 1fr 1fr 56px 18px;
	gap: 4px;
	font-size: 10px;
	font-weight: 700;
	color: var(--blue);
	text-transform: uppercase;
	letter-spacing: 0.3px;
	padding-bottom: 4px;
	border-bottom: 1px solid var(--silver);
	margin-bottom: 4px;
}
.pt-row {
	display: grid;
	grid-template-columns: 16px 1fr 1fr 1fr 56px 18px;
	gap: 4px;
	align-items: center;
	padding: 6px 0;
	border-bottom: 1px solid var(--silver);
}
.pt-row:last-of-type { border-bottom: none; }
.pt-row-over { background: #FDECEA; border-radius: 4px; }
.pt-num {
	font-weight: 700;
	font-size: 12px;
	color: var(--blue);
}
.pt-input-group {
	background: white;
	border-radius: 6px;
	padding: 4px 6px;
	display: flex;
	flex-direction: column;
	gap: 2px;
}
.pt-input-group label {
	font-size: 9px;
	text-transform: uppercase;
	color: var(--muted);
	font-weight: 700;
}
.pt-input {
	width: 100%;
	padding: 3px 4px;
	font-size: 13px;
	border: 1px solid var(--border);
	border-radius: 4px;
}
.pt-bags {
	font-size: 10px;
	color: var(--blue);
	font-weight: 600;
	text-align: right;
}
.pt-total {
	text-align: center;
	font-size: 12px;
	font-weight: 700;
	color: var(--blue);
}
.pt-status {
	font-size: 9px;
	font-weight: 600;
	color: var(--green);
}
.pt-row-over .pt-status { color: var(--red); }

.trolleyplan-card {
	background: white;
	border: 1px solid var(--border);
	border-radius: 10px;
	margin: 0 16px 12px;
	padding: 14px 16px;
	position: relative;
	box-shadow: 0 1px 2px rgba(18,20,28,0.05), 0 4px 14px rgba(18,20,28,0.07);
}
.trolleyplan-card::before {
	content: '';
	position: absolute;
	top: 0; left: 10px; right: 10px;
	height: 1px;
	background: linear-gradient(90deg, transparent, rgba(0,60,255,0.35), transparent);
}
.trolleyplan-title {
	font-size: 13px;
	font-weight: 700;
	color: var(--blue);
	margin-bottom: 10px;
}
.tp-warning {
	background: #FDECEA;
	border: 1px solid var(--red);
	color: #8a2419;
	border-radius: 6px;
	padding: 8px 10px;
	margin-bottom: 10px;
	font-size: 12px;
	font-weight: 600;
	line-height: 1.4;
}

.tp-req-section {
	background: white;
	border-radius: 6px;
	padding: 8px 10px;
	margin-bottom: 10px;
}
.tp-req-title {
	font-size: 11px;
	font-weight: 700;
	color: var(--blue);
	text-transform: uppercase;
	letter-spacing: 0.3px;
	margin-bottom: 4px;
}
.tp-req-row {
	display: flex;
	justify-content: space-between;
	font-size: 13px;
	padding: 2px 0;
}
.tp-req-row b { color: var(--blue); }
.tp-header {
	display: grid;
	grid-template-columns: 20px 1fr 1fr 46px 20px;
	gap: 6px;
	font-size: 11px;
	font-weight: 700;
	color: var(--blue);
	text-transform: uppercase;
	letter-spacing: 0.3px;
	padding-bottom: 6px;
	border-bottom: 1px solid var(--silver);
	margin-bottom: 4px;
}
.tp-row {
	display: grid;
	grid-template-columns: 20px 1fr 1fr 46px 20px;
	gap: 6px;
	align-items: center;
	padding: 6px 0;
	border-bottom: 1px solid var(--silver);
	font-size: 13px;
}
.tp-row:last-of-type { border-bottom: none; }
.tp-row-readonly { opacity: 0.85; }
.tp-readonly-label { font-size: 12px; color: var(--blue); }
.tp-polony-header { margin-top: 10px; }
.tp-num {
	font-weight: 700;
	color: var(--blue);
}
.tp-half {
	background: white;
	border-radius: 6px;
	padding: 4px 6px;
	display: flex;
	flex-direction: column;
	gap: 2px;
}
.tp-select {
	width: 100%;
	padding: 4px;
	font-size: 12px;
	border: 1px solid var(--border);
	border-radius: 4px;
	background: white;
}
.tp-bags {
	font-weight: 700;
	color: var(--blue);
	font-size: 11px;
	text-align: right;
}
.tp-empty {
	color: var(--muted);
	font-style: italic;
	font-size: 12px;
}
.tp-total {
	text-align: center;
	font-weight: 700;
	color: var(--blue);
	font-size: 12px;
}
.tp-remove {
	text-align: center;
	color: var(--red);
	font-weight: 700;
	cursor: pointer;
	font-size: 14px;
}
.tp-actions {
	display: flex;
	flex-wrap: wrap;
	gap: 8px;
	margin-top: 10px;
}
.tp-btn {
	flex: 1;
	min-width: 100px;
	padding: 8px;
	border: 1px solid var(--blue);
	border-radius: 6px;
	background: white;
	color: var(--blue);
	font-size: 12px;
	font-weight: 700;
	cursor: pointer;
}
.tp-btn-reset { background: var(--platinum); }
.tp-btn-save { background: linear-gradient(135deg, var(--blue), var(--blue-deep)); color: white; box-shadow: 0 1px 2px rgba(18,20,28,0.08), 0 3px 8px rgba(18,20,28,0.12); }

.unit-hint {
	text-align: center;
	font-size: 10px;
	color: var(--muted);
	font-weight: 600;
	margin-top: 1px;
	text-transform: uppercase;
	letter-spacing: 0.3px;
}

.coldroom-input {
	margin-top: 6px;
	border-color: var(--blue);
	background: var(--platinum);
}

.stock-na {
	text-align: center;
	font-size: 12px;
	color: var(--muted);
	font-style: italic;
}

.action-bar {
	position: fixed;
	bottom: 0; left: 0; right: 0;
	background: white;
	border-top: 1px solid var(--border);
	padding: 12px 16px;
	display: flex;
	gap: 10px;
}
.btn {
	flex: 1;
	padding: 14px;
	border: none;
	border-radius: 10px;
	font-size: 15px;
	font-weight: 700;
	cursor: pointer;
	position: relative;
	box-shadow: 0 1px 2px rgba(18,20,28,0.08), 0 3px 8px rgba(18,20,28,0.12);
}
.btn-print { background: linear-gradient(135deg, var(--charcoal), var(--steel)); color: white; }
.btn-refresh { background: var(--platinum); color: var(--charcoal); }
.btn-save { background: linear-gradient(135deg, var(--blue), var(--blue-deep)); color: white; }
.btn-save:disabled { background: #ccc; box-shadow: none; cursor: not-allowed; }

.toast {
	position: fixed;
	top: 16px; left: 50%;
	transform: translateX(-50%);
	background: var(--dark);
	color: white;
	padding: 12px 20px;
	border-radius: 8px;
	font-size: 14px;
	font-weight: 600;
	z-index: 999;
	opacity: 0;
	transition: opacity 0.25s;
	pointer-events: none;
	max-width: 90%;
	text-align: center;
}
.toast.show { opacity: 1; }
.toast.success { background: var(--green); }
.toast.error { background: var(--red); }

#printArea { display: none; }

@media print {
	.no-print { display: none !important; }
	#mainContent { display: none !important; }
	#printArea { display: block !important; }
	body { background: white; }
}

#printArea {
	font-family: 'Poppins', -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
}

.pp-header {
	display: flex;
	justify-content: space-between;
	align-items: flex-start;
	border-bottom: 2px solid var(--blue);
	padding-bottom: 10px;
	margin-bottom: 16px;
}
.pp-logo {
	font-size: 26px;
	font-weight: 800;
	color: var(--blue);
	letter-spacing: 0.5px;
}
.pp-logo span { color: var(--dark); }
.pp-route {
	font-size: 18px;
	font-weight: 700;
	color: var(--dark);
	margin-top: 2px;
}
.pp-period {
	display: inline-block;
	margin-left: 8px;
	padding: 2px 8px;
	border-radius: 10px;
	background: var(--blue);
	color: white;
	font-size: 10px;
	font-weight: 700;
	vertical-align: middle;
}
.pp-date {
	text-align: right;
	font-size: 15px;
	font-weight: 700;
	color: var(--dark);
}
.pp-day {
	font-size: 12px;
	font-weight: 400;
	color: var(--muted);
	margin-top: 2px;
}

.pp-columns {
	display: flex;
	gap: 20px;
	margin-bottom: 18px;
}
.pp-columns > div { flex: 1; }

.pp-table {
	width: 100%;
	border-collapse: collapse;
	font-size: 11px;
}
.pp-table thead th {
	background: var(--dark);
	color: white;
	text-align: left;
	padding: 5px 6px;
	font-size: 10px;
	text-transform: uppercase;
	letter-spacing: 0.3px;
}
.pp-table thead th:nth-child(3),
.pp-table thead th:nth-child(4),
.pp-table thead th:nth-child(5),
.pp-table thead th:nth-child(6) { text-align: center; }
.pp-table tbody tr:nth-child(even) { background: #f2f2f2; }
.pp-table td {
	padding: 4px 6px;
	border-bottom: 1px solid #ddd;
}
.pp-code { color: var(--muted); font-size: 10px; width: 30px; }
.pp-name { font-weight: 600; }
.pp-produce { text-align: center; font-weight: 700; width: 36px; }
.pp-pack { text-align: center; font-weight: 700; width: 36px; color: var(--dark); }
.pp-labels { text-align: center; font-size: 10px; color: var(--muted); width: 34px; }
.pp-actual {
	text-align: center;
	width: 36px;
	border: 1px solid #999 !important;
	background: white;
}

.pp-box {
	border: 1px solid var(--blue);
	border-radius: 8px;
	overflow: hidden;
	margin-top: 6px;
}
.pp-box-header {
	background: linear-gradient(135deg, var(--blue), var(--blue-deep));
	color: white;
	font-weight: 700;
	font-size: 13px;
	padding: 8px 12px;
}
.pp-box-body { padding: 12px; }
.pp-subtitle {
	font-size: 12px;
	font-weight: 700;
	color: var(--dark);
	margin: 14px 0 6px;
	text-transform: uppercase;
	letter-spacing: 0.3px;
}
.pp-trolley-table {
	width: 100%;
	border-collapse: collapse;
	font-size: 12px;
}
.pp-trolley-table thead th {
	background: var(--platinum);
	text-align: left;
	padding: 5px 8px;
	font-size: 10px;
	text-transform: uppercase;
	color: var(--muted);
}
.pp-trolley-table td {
	padding: 6px 8px;
	border-bottom: 1px solid #ddd;
}
.pp-tnum { font-weight: 700; width: 24px; }
.pp-empty { font-size: 12px; color: var(--muted); font-style: italic; padding: 4px 0; }

.pp-footer {
	display: flex;
	justify-content: space-between;
	margin-top: 16px;
	font-size: 9px;
	color: var(--muted);
}
